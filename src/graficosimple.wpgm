import wollok.game.*
import grafico.*


program graficosimple {
	game.title("Gráfico de barras")
	game.height(15)
	game.width(15)

	new GraficoBarras(valores = [10,7,1,4], etiquetas=["perro","gato","león","pato"], position = game.at(3,3)).dibujar()
	game.start()
}

